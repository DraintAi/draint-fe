{
  "name": "@draint/snap",
  "version": "0.1.0",
  "description": "drain't MetaMask Snap — pre-sign warning for EIP-7702 delegation drainers and Permit phishing. Wallet drain? Didn't happen.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DraintAi/draint-fe.git"
  },
  "homepage": "https://draint.vercel.app",
  "license": "MIT",
  "main": "./dist/bundle.js",
  "files": [
    "dist/",
    "snap.manifest.json",
    "images/"
  ],
  "scripts": {
    "build": "mm-snap build",
    "clean": "rimraf dist",
    "build:clean": "bun run clean && bun run build",
    "serve": "mm-snap serve",
    "start": "mm-snap watch",
    "manifest": "mm-snap manifest --fix"
  },
  "dependencies": {
    "@metamask/snaps-sdk": "~10.3.0"
  },
  "devDependencies": {
    "@metamask/snaps-cli": "^8.3.0",
    "rimraf": "^5.0.10",
    "typescript": "~5.7.3"
  },
  "engines": {
    "node": ">=18.6.0"
  }
}
